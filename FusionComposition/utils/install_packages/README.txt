To install packages open conda terminal and navigate to directory that script is saved and run this command to make script executable:

chmod +x install_packages.sh

Then execute the script with this command:

./install_packages.sh